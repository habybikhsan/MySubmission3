<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activity.FavoriteActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycleViewFavorite"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/item_row_usersfavorite" />

    <ProgressBar
        android:id="@+id/progressBarFavorite"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@+id/recycleViewFavorite"
        app:layout_constraintEnd_toEndOf="@+id/recycleViewFavorite"
        app:layout_constraintStart_toStartOf="@+id/recycleViewFavorite"
        app:layout_constraintTop_toTopOf="@+id/recycleViewFavorite" />
</androidx.constraintlayout.widget.ConstraintLayout>